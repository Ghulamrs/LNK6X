; q01: one code section and nothing else. Whatever lnk6x writes here is what an image costs
; before it does anything at all - the segments, the alignment, the symbols it invents unasked.
	.global _c_int00
	.text
_c_int00:
	NOP
	B B3
	NOP 5
